# Source Tree

```text
squads/motion-squad/
  squad.yaml
  agents/
  tasks/
  workflows/
  checklists/
  templates/
  config/
  data/
  tools/
  scripts/
```

Runtime execution remains in the host project's isolated `.motion/remotion/` workspace.

The Squad governs decisions and gates. The Motion Agent runtime performs technical rendering and QA.
